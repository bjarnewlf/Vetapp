---
paths:
  - "src/context/SubscriptionContext.tsx"
  - "src/screens/PaywallScreen.tsx"
  - "src/screens/ProfileScreen.tsx"
---

# Premium / Paywall

The `isPro` flag from `SubscriptionContext` gates certain features (e.g., document uploads). `PaywallScreen` handles the upgrade flow.
