# Agency-Briefing — VetApp, 03.04.2026

## Erstellt von: Brian (Developer, Designer, QA)

---

## Heutiger Fortschritt

### Commits heute (3 Stück, noch nicht gepusht)
1. `3e6a8ef` — chore: Claude Code Konfiguration (CLAUDE.md, Agent-Definitionen, Settings)
2. `21cb33e` — feat: Push-Notifications, Datei-Upload, erweiterte Event-Verwaltung, Abo-Persistenz
3. `dd70571` — fix: Deutsche Tierarten-Labels, personalisierte Begrüßung, Treatments-Daten, Löschen-UI

### Was heute erledigt wurde
- Alle offenen Änderungen sauber committet (2 Commits: Infrastruktur + Features)
- 5 Bugfixes umgesetzt und von QA abgenommen:
  - HomeScreen: Echte Treatment-Daten statt Placeholder
  - Tierarten überall auf Deutsch (zentrales Mapping)
  - Personalisierte Begrüßung ("Hallo, {Name}!")
  - Impfungen & Behandlungen im PetDetail löschbar mit Confirm-Dialog
  - Recurrence-Anzeige bei Impfungen auf Deutsch
- QA-Finding M-1 gefixt: Vaccination.recurrenceInterval Typ von string auf RecurrenceType

---

## TypeScript: 0 Fehler

---

## KRITISCH (sofort angehen)

### K1: Supabase-Credentials hardcoded im Repo
- Wo: src/lib/supabase.ts Z.5-6
- Was: URL und Anon-Key als Plaintext. Gehört in .env + .gitignore, einbinden via expo-constants.

### K2: user!.id Non-Null-Assertion
- Wo: src/context/DataContext.tsx Z.217
- Was: completeReminder prüft user nicht vor dem Insert des Folge-Reminders. Alle anderen Mutations tun das. Crash-Risiko bei Race Condition nach Logout.

---

## MITTEL (vor Release fixen)

### M1: Signierte URLs laufen ab
- Wo: src/utils/fileUpload.ts Z.31 + DataContext.tsx
- Was: Dokument-URLs mit 1-Jahr-Signatur in DB gespeichert. Danach kaputte Links ohne Fehlermeldung. Besser: Storage-Pfad speichern, URL on-demand generieren.

### M2: Keine Datums-Validierung
- Wo: AddEventScreen.tsx Z.79-83, EventDetailScreen.tsx Z.46-49
- Was: "32.13.2025" wird zu NaN-NaN-NaN an Supabase geschickt.

### M3: Doppelte Submits
- Wo: AddEventScreen, AddReminderScreen, AddPetScreen
- Was: Kein Loading/Disabled-State bei Save-Buttons. Schnelles Doppeltippen = doppelte Einträge.

### M4: Stille Fehler
- Wo: DataContext.tsx Z.160, 181, 194, 254ff
- Was: Pattern "if (!error) await refresh()" — bei Fehler passiert nichts. User bekommt keine Rückmeldung. Nur addDocument wirft Fehler weiter.

### M5: Dev-Toggle in Production
- Wo: ProfileScreen.tsx Z.52-66
- Was: "Premium-Modus"-Switch lässt jeden User sich selbst auf Premium setzen. Schreibt is_premium: true direkt nach Supabase.

### M6: Fragile Storage-URL-Extraktion
- Wo: DataContext.tsx deleteDocument
- Was: Parst Storage-Pfad per Regex aus Signed URL. Kann bei URL-Struktur-Änderung brechen. Besser: Pfad als eigenes DB-Feld.

### M7: DataContext ist God-Object
- Was: ~350 Zeilen, 15+ Funktionen. Noch okay, skaliert aber nicht.

---

## DESIGN & UX

### D1: Null Accessibility
- Kein einziger accessibilityLabel. Touch-Targets zu klein (Trash-Icons 18px statt min. 44px).

### D2: Tote Buttons
- "Behandlungen"-Button in PetDetail (kein onPress), Stift-Icon in ProfileScreen (führt nirgendwo hin).

### D3: Custom-Picker 4x dupliziert
- AddPetScreen, AddEventScreen, etc. — sollte eine SelectField-Komponente werden.

### D4: Inkonsistente Farben
- ProfileScreen: Hardcoded #f5f0e8 statt Theme. Shadow-Farbe #000 statt Theme-Variable, 5x dupliziert.

### D5: Kein Loading-State
- Kein Indikator beim initialen Datenladen aus Supabase. Kein Error-State bei Fehlern.

### D6: getAge('') = "NaN Jahre"
- Bei fehlendem Geburtsdatum. getAge() ist außerdem in PetDetailScreen und MyPetsScreen dupliziert.

---

## Was gut läuft
- Architektur sauber und dokumentiert
- Supabase RLS korrekt konfiguriert
- Design-System überwiegend konsistent genutzt
- TypeScript strict ohne Fehler
- Dependencies aktuell und schlank
- Notifications sauber implementiert

---

## Empfohlene Reihenfolge
1. K1 + K2 — Credentials raus, Null-Check rein (~30 Min)
2. M5 — Dev-Toggle entfernen (trivial, Security-relevant)
3. M2 + M3 — Datums-Validierung + Doppel-Submit-Schutz
4. M4 — Error-Feedback bei Mutations
5. D1 + D2 — Accessibility Basics + tote Buttons

---

## Housekeeping
- .claude/rules/ — noch ungetrackt, sollte committet werden
- 227d64a25639f660b6ba6ad086f0b85c.webp — temporäres Bild im Root, gehört nicht ins Repo
- 3 Commits lokal, noch nicht gepusht
